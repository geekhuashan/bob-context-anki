# Changelog

## 0.6.0 - 2026-08-28

- 优先下载目标词在 Free Dictionary API 中明确标记为 US English 的真人发音；词条提供 Wikimedia Commons 原始页时，使用该页的同录音 MP3 转码地址。
- 拒绝英音、未标明美音、非 HTTPS 和非 MP3 的词典音频；查询、下载或内容异常时自动回退到 MiniMax。
- 发音媒体文件名记录 `dictionary-us` 或 `minimax` 来源，Bob 的完成提示会显示实际使用的来源。
- 真人录音的来源页、录音者和许可会写入卡片 `Source` 字段，满足音频条目的署名要求。
- `0.5.x` 的旧版 MiniMax 音频会在下一次点击对应词时迁移一次；其他自定义音频保持不变。
- 词典请求不携带 API Key，MiniMax Key 只在兜底请求中发送。

## 0.5.0 - 2026-08-28

- 使用 MiniMax `speech-2.8-hd` 为目标词生成英语 MP3 发音并存入 Anki 媒体库。
- 新卡的注释和发音并行生成，全部成功后才创建词卡。
- 支持原地修复本插件旧卡的空 `Audio` 字段，且不会重新请求已有的注释。
- 完整重复卡不请求注释或发音服务；远程生成或媒体写入失败时不创建半成品卡片。
- 增加独立的安全发音配置，留空发音 Key 时复用注释 Key。

## 0.4.0 - 2026-08-28

- 新卡片写入前通过可配置的 OpenAI 兼容接口生成音标、英释、中释、语境义和整句翻译。
- 先查重再请求注释，重复词不消耗 API 额度。
- 支持补全本插件先前创建的空注释卡，不修改其他来源的重复卡片。
- 注释配置缺失、请求失败或返回格式异常时不创建空白卡片。
- API Key 使用 Bob 的安全输入设置，不进入源码或发布包。

## 0.3.2 - 2026-08-28

- 将完整 MIT 许可和第三方来源声明嵌入可独立分发的插件主文件。
- 增加发布包致谢完整性检查。

## 0.3.1 - 2026-08-28

- 过滤公式中的单字母变量。
- 正确处理直撇号和弯撇号英语缩写，避免出现 `ve` 等残片。
- 增加真实数学句子的回归测试。

## 0.3.0 - 2026-08-28

- 使用 CEFR-J Wordlist 1.6 对候选词分级。
- 默认只显示 B2 及以上和词表外术语。
- 支持最低等级设置和个人“已认识词”排除列表。

## 0.2.0 - 2026-08-28

- 点击候选词后，将该词和原句写入 `Vocabulary Modern` 笔记。
- 增加两分钟、一次性的上下文匹配会话。
- 写入前通过 AnkiConnect 检查重复。
- 移除外部翻译请求、API Key、文件回退和后台辅助进程。

## 0.1.0 - 2026-08-28

- 验证 Bob 的 `relatedWordParts` 候选词可以触发后续单词查询。
